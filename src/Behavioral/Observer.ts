/*
    One-to-many transfer of data.
*/

// import { Subject } from 'rxjs';

// const news = new Subject();

// const tv1 = news.subscribe((v: any) => console.log(v + 'via Den TV'));
// const tv2 = news.subscribe((v: any) => console.log(v + 'via Batcave TV'));

// news.next('Breaking news.');
// news.next('The war is over.');